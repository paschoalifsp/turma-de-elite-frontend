export default interface postDeliveryActivity {
  className: string;
  postActivity: number;
  deliveryActivity: number;
}

export interface studentPunctuation {
  studentName: string;
  punctuation: number;
}